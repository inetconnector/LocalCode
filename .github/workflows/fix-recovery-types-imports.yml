name: Fix Recovery Types Imports

on:
  push:
    branches: [agent/durable-run-recovery]

permissions:
  contents: write

concurrency:
  group: fix-recovery-types-imports
  cancel-in-progress: false

jobs:
  patch:
    runs-on: windows-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - name: Apply import fix
        shell: pwsh
        run: |
          python scripts/fix-recovery-types-imports.py
          gofmt -w src/types.go
          git diff --check
      - name: Commit import fix
        shell: pwsh
        run: |
          if (-not (git status --porcelain -- src/types.go)) { exit 0 }
          git config user.name 'localcode-ci'
          git config user.email 'localcode-ci@users.noreply.github.com'
          git add -- src/types.go
          git commit -m 'fix: import recovery path helpers'
          git push origin HEAD:agent/durable-run-recovery
