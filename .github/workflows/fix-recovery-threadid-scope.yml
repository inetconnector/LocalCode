name: Fix Recovery Thread Scope

on:
  push:
    branches: [agent/durable-run-recovery]

permissions:
  contents: write

concurrency:
  group: fix-recovery-threadid-scope
  cancel-in-progress: false

jobs:
  patch:
    runs-on: windows-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - name: Apply scope fix
        shell: pwsh
        run: |
          python scripts/fix-recovery-threadid-scope.py
          gofmt -w src/agent.go
          git diff --check
      - name: Commit scope fix
        shell: pwsh
        run: |
          if (-not (git status --porcelain -- src/agent.go)) { exit 0 }
          git config user.name 'localcode-ci'
          git config user.email 'localcode-ci@users.noreply.github.com'
          git add -- src/agent.go
          git commit -m 'fix: reuse requested thread id for recovery journal'
          git push origin HEAD:agent/durable-run-recovery
