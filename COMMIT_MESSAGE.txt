fix(windows): harden runtime bootstrap and test isolation

- isolate Windows tests from real user profiles, PATH entries and project folders
- terminate Aider and MCP subprocess trees deterministically on cancellation
- install and verify missing Ollama, configured models, uv, Python 3.12 and Aider
- resolve symlinks and NTFS junctions before enforcing project sandbox boundaries
- prevent DNS-rebinding bypasses by dialing the already validated public IP
- require a supported Go toolchain and rebuild automatically when sources are newer
- add runtime bootstrap, model pull, migration and security regression tests
- update German and English documentation for LocalCode 6.1.0
