fix: add deterministic task supervision and context compaction

- route analysis, builds, Android deployment, Git setup and web research through verified actions
- prevent irrelevant Git initialization and file mutations during read-only analysis
- execute confirmed Git initialization directly and verify the repository
- derive non-empty web queries and add a fallback search provider
- discover, install, verify and retry supported development tools
- compact long agent contexts while preserving task state, decisions, files, commands and failures
- expose compaction controls and events in the settings and output UI
- add regression tests for agent drift, Git continuation and long-context recovery
- update documentation, STATE.md and release metadata for LocalCode 4.7.0
