feat: add complete bilingual UI and persistent approval workflow

- detect the Windows display language and default to German or English
- add manual language selection and synchronized translation catalogs
- translate project templates, Git setup, documentation, and runtime UI
- keep pending approvals visible in a bottom-center decision bar across views
- improve project-root selection with direct path entry and a foreground dialog
- normalize numeric settings before saving
- persist config and chat history with recoverable atomic replacement
- move chat event persistence to a coalescing background writer
- add race, localization, approval, root-selection, and persistence tests
