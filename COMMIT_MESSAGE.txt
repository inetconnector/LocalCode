feat(engines): add selectable Aider, Claude Code, and OpenCode backends

- introduce a shared coding-engine abstraction for edit, repo-map, lint, test,
  setup, authentication, cancellation, backup, and undo workflows
- add Claude Code native Windows installation, status/login checks, bounded
  non-interactive execution, model/channel settings, and safe permission modes
- add user-local OpenCode installation through managed Node.js/npm, provider/model
  settings, authentication, auto-approval control, and process-scoped Ollama config
- preserve the complete Aider integration and legacy aider_* action aliases
- add engine-specific settings, status controls, German/English translations, and
  UI/API regression coverage for switching and testing all three engines
- keep statement coverage above 80 percent and retain race, vet, randomized,
  Windows cross-build, manifest, and checksum release verification
